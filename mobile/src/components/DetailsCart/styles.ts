import { StyleSheet } from "react-native";

export default StyleSheet.create({
  centeredView: {
    alignItems: "center",
    justifyContent: "center",
    backgroundColor: "#fff",
  },
  textSubtotal: {
    fontSize: 20,
    fontWeight: "bold",
    paddingVertical: 2,
  },
  textLinkButton: {
    color: "#FFFFFF",
    textAlign: "center",
    fontSize: 16,
  },
});
