import { StyleSheet } from "react-native";

export default StyleSheet.create({
  centeredView: {
    flex: 1,
    alignItems: "center",
    justifyContent: "center",
  },
  message: {
    fontSize: 20,
  },
  viewIcon: {
    flexDirection: "row",
    alignItems: "flex-end",
  },
  warningIcon: {
    marginBottom: 8,
    right: 8,
  },
});
